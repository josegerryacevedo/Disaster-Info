<!DOCTYPE html>
<html>
  <head>
    <title>App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="container">
    <p><%= t( "hello", :name => current_user&.email)  if current_user %> </p>
    <%= link_to "English", params.permit!.merge(locale: 'en') %>
    <%= link_to "Tagalog", params.permit!.merge(locale: 'tl') %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= link_to 'Admin', admin_types_path if current_user&.admin? %>
    <%= yield %>
  </body>
</html>
